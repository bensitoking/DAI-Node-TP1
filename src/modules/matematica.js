const PI = 3.14;
function sumar(x, y) {
let total = x + y;
 return total;
}
const multiplicar = (x, y) => {
    let total = x * y;
    return total;
}
function dividir (x, y) {
    
    let total = x / y;
    return total;
}
const restar = (x, y) => {
    let total = x - y;
    return total;
}
export {PI, sumar, multiplicar, dividir, restar};

